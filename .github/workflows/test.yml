name: Test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Steup node
        uses: actions/setup-node@v3
        with:
          node-version: "14"
      - run: npm ci
      - run: npm test
